import React from 'react';
import { connect } from 'react-redux';
import './App.css';

function App(props) {
  return <div>order</div>
}

export default connect(
  state => { return {} },
  dispatch => {return {}}
)(App);